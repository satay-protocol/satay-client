import { APT } from "./coinStructs";

import { Coin } from "../types/coin";

export const coins: Coin[] = [
    {
        name: "Aptos",
        symbol: "APT",
        protocol: "aptos",
        coinStruct: APT
    }
]
